# dropheus frontend

This is the frontend for dropheus, which is a chrome extension that allows you to select an etsy item and check if their are matching items on AliExpress, to detect dropshipping, or find cheaper alternatives.

This frontend sends in requests to /search?q=searchTerm, where it then returns it's response to show the user either alternative, with their urls, prices, and images.
